# PhotoCut 变更日志

## V8.4 / 软件包 0.2.0 — 2026-09-06

默认检测器升级为 V8.4：一个网络预测四边位置分布，通过自身位置方差加权求四角。发布可再分发的 ONNX 推理模型，不依赖 PyTorch，不运行旧模型选择或结果救回。合法结果进入 GUI 2.0 人工确认；非法或未生成有效候选的输入拒绝输出裁剪坐标，当前界面可跳过。GUI 版本保持 2.0。

全部 320 张训练后的合法严格候选由 306 提高到 314（新增 10、退化 2），严重错误 0、非法结果 3。来源分组对照从 291 提高到 294（新增 9、退化 6）。前者是训练集成绩，后者也是历史复用数据，均不能作为独立盲测结论。未满足保住旧 306 张、非法归零等原研究替换条件；此次根据维护者明确决定接入主流程，以人工确认为使用前提。完整指标与限制见 [V8.4 说明](docs/V8.4.md)。

旧 `--detector auto`、`v8`、`v7`、`v5.2` 继续提供显式回滚；`generic_single` 场景须配合 `--detector auto`。原有 `auto-v4` 身份及 V8.2 算法、模型不改写；共享宿主代码改变后，重新核验并更新兼容策略的代码封印。公开仓库新增 ONNX 部署资产、运行代码、合成测试与文档；不发布照片、标注、PyTorch 训练检查点或逐图实验记录。

同一输出目录升级后可继续修订旧确认：使用显式原批次引用追加标注，不复制标注 ID 或覆盖历史事件；裁剪和重开恢复保持正确的标注来源。

## v8.3.0-single-model-research1 — 2026-09-05

以下为当时的研究检查点状态；当前默认版本已由上方 V8.4 发布条目取代。

保存单模型四角检测的阶段研究结论：一个网络预测四边位置分布，以自身位置方差加权拟合边线并求四角。默认 PhotoCut Selector v4、V8.2、GUI 2.0 与软件包 0.1.0 保持原有行为。

已知 320 张扫描照片中，新模型合法严格候选 306（95.6%），旧默认流程自动严格结果 249；配对新增 64、退化 7，新模型非法结果 3、严重错误 0。严格要求合法四边形且四角最大误差不超过图像对角线 0.5%；严重错误指四角平均误差超过 3%。新模型指标是候选质量，尚未校准自动接受策略，不能直接解释为自动化率。

样本含 176 张训练照片、44 张用于选模的校准照片及 100 张反复使用的回归照片；分别严格 174、41、91。因此此结果不是独立盲测，不代表对新扫描照片的泛化准确率，也不声称已经解决全部角点问题。

本公开检查点只包含脱敏研究摘要。研究代码、详细日志单独保存在私有研究仓库；照片、标注、模型训练权重与逐图诊断留在本机，不随此次公开或私有 Git 推送。当前用户仍可直接运行仓库内既有 V8.2 模型。后续工作集中在单模型的退化、几何合法性、自动接受校准及实际流程接入。

发布检查：现有完整测试 813 项通过，358 个子测试通过；公开树隐私检查通过。

## 0.1.0

首个公开版本：检测、浏览器确认、透视校正与裁剪，附带可再分发 V8.2 ONNX 模型。
